# Rust version to compile in rustc
cargo(rustc) version 1.58.0

# Rust latest verion(1.66.0) so far.

# Rust downgrade to 1.58.0

$>. ~/.cargo/env
$>rustup default 1.58.0
$>rustup install 1.58.0

$>. ~/.cargo/env

# Rust version check
$>cargo --version, or rustc --version

